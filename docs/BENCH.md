# KPI ベースライン計測記録（Phase 0）

| 項目     | 内容                                                                                                                                                                |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 文書番号 | NEO‑BENCH‑2026‑001                                                                                                                                                  |
| 計測日   | 2026‑09‑23                                                                                                                                                          |
| 対象     | `rikunarita/ComfyUI-Model-Manager-Neo` @ dev（Phase 0 着手時）                                                                                                      |
| 目的     | `Agent/Plan.md` §2.2 KPI 表の「現行」列を**実測**で確定し、以降のフェーズ完了判定の比較原点とする（§5.2「Phase 0 で全 KPI のベースラインを docs/BENCH.md に記録」） |
| 生データ | [`scripts/bench/results/`](../scripts/bench/results/)（本実行の JSON 一式）                                                                                         |
| 再実行   | [`scripts/bench/README.md`](../scripts/bench/README.md)（参照機での再計測手順を含む）                                                                               |

> **読み方の注意**: 本計測は Plan §2.2 の検証環境（「8C/16T デスクトップ（NVMe）」）ではなく、
> **2 vCPU / 1 GiB RAM のコンテナ**で実行した（下記環境表）。絶対値の比較には注意が必要だが、
> (1) ピーク RAM の**倍率**（入力の何倍か）、(2) スループットの**相対比**（経路間・アルゴリズム間）、
> (3) 欠陥の**再現性**は環境に依存せず結論づけられる。スクリプトはすべて参照機でそのまま
> 再実行できる形にしてあり、結果 JSON には実行環境（`env`）が埋め込まれる。

---

## 1. 計測環境（実測値）

| 項目             | 値                                                                                                                                                                              |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CPU              | Intel Xeon @ 2.50 GHz（Skylake‑SP、**2 vCPU**、AVX‑512 あり、**SHA 拡張なし**）                                                                                                 |
| RAM              | 1 GiB（cgroup ハード上限、swap なし）                                                                                                                                           |
| ストレージ       | overlay on virtio‑fs（Kata コンテナ。env‑report 実測: 逐次書込 ~627 MB/s）                                                                                                      |
| OS               | Debian 12（glibc 2.36）、ホストカーネル 4.19.91                                                                                                                                 |
| Python           | 3.11.2（torch 2.14.0+cpu / numpy 2.4.6 / safetensors 0.8.0 / blake3 1.0.9）                                                                                                     |
| 計測対象コア     | `third_party/zipnn-core-bin/linux-x86_64/zipnn_core.cpython-311-x86_64-linux-gnu.so`（ZipNN 0.5.4 同梱、101,144 B）                                                             |
| Rust             | 1.98.1 stable（mold 1.10.1 / clang 14 / zig 0.16.0 / maturin 1.15.0 / cargo‑zigbuild 0.23.4）                                                                                   |
| 実モデル         | `unsloth/all-MiniLM-L6-v2` model.safetensors（ModelScope 経由、90,868,376 B、f32×104 テンソル）                                                                                 |
| 合成フィクスチャ | Gaussian 重み（σ=0.02）: bf16/f32/f16/fp8e4m3 各 192 MB、MoE ヘッダー 8.42 MB（64,491 テンソル）、デルタペア 32 MB×2、5,000 モデルツリー（7,532 ファイル）、ハッシュ対象 256 MB |

計測方法: 各計測はサブプロセスに隔離し、カーネル記録のピーク RSS（`VmHWM`）と
実時間（monotonic）を報告する（`scripts/bench/common.py`）。圧縮スレッド数は
vendored ZipNN と同じ `min(cpu_count, 16)` = **2**。

---

## 2. JSON パーサ選定結果（Phase 0 の決定事項）

Plan §3.7 の第一候補 **jiter 0.17.0** vs 比較候補 simd‑json 0.18.1（serde_json 1.0.151 は参考）。
8.42 MB（8.03 MiB）の合成 MoE ヘッダー（64,491 テンソル）を「全テンソルの dtype/shape を
走査してダイジェスト化する」という実タスク（`get_model_tensors` 相当）で計測。
3 パーサのダイジェストは完全一致（正しさの相互検証）。中央値 / 7 反復:

| パーサ     | min      | median      | mean     | スループット  | 非破壊借用解析（read‑only mmap 要件）         |
| ---------- | -------- | ----------- | -------- | ------------- | --------------------------------------------- |
| **jiter**  | 10.2 ms  | **10.6 ms** | 10.7 ms  | **757 MiB/s** | **○**（`&[u8]` 借用、コピー不要）             |
| simd‑json  | 184.8 ms | 187.6 ms    | 190.3 ms | 43 MiB/s      | **×**（in‑place 変換 = 毎回の可変コピー必須） |
| serde_json | 141.9 ms | 143.6 ms    | 165.2 ms | 56 MiB/s      | ○（ただし DOM 構築 = 全データmaterialize）    |

**決定: jiter 0.17.0**（simd‑json 比 **約 17.7 倍**、serde_json 比 約 13.5 倍）。
ストリーミング借用解析が DOM 構築を圧倒し、かつ mmap 要件（Plan §4.3「ヘッダーは
mmap バイトを非破壊・借用解析」）を満たすのは jiter のみ。K11 目標（≤40 ms）に対して
パース単体は既に **10.6 ms** で、目標は現実的であることが示された。
（計測器: `native/benches/json-bench`、出力: `scripts/bench/results/json-bench.txt`）

---

## 3. KPI ベースライン総表（K1–K16）

「現行実測」= 本計測、「Plan 見込み」= Plan §2.2 の策定時見積り。目標は Plan のまま。

| #   | 指標                                 | 現行実測（本環境）                                                                                                                                                                                        | Plan 見込み            | 目標                        | 備考                                                                                                        |
| --- | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | --------------------------- | ----------------------------------------------------------------------------------------------------------- |
| K1  | 圧縮ピーク RAM（12 GB bf16）         | 入力の **2.2–2.6 倍**（実測 32–192 MB で線形性を確認。e2e: 64 MB bf16 で +168 MiB=2.6×、実モデル 86.7 MB f32 で +215 MiB=2.5×）→ 12 GB 換算 **約 27–31 GB**                                               | 24 GB 超               | < 1 GB                      | 実測倍率は見込みよりやや悪い                                                                                |
| K2  | 圧縮スループット                     | C コア直呼び 337–800 MB/s（bf16 192 MB: 395 MB/s）。**Neo e2e: 141–172 MB/s**（torch/Python 層が 2.3–2.8 倍の律速）                                                                                       | —                      | ≥ 1.5 倍（Rust）            | 比較基点は e2e 値                                                                                           |
| K3  | 解凍スループット                     | C コア 667–1,722 MB/s（bf16 192 MB: 1,005 MB/s）。**Neo e2e: 207–289 MB/s**                                                                                                                               | —                      | ≥ 2 倍（Rust）              | 同上                                                                                                        |
| K4  | デルタ圧縮ピーク RAM（12 GB ペア）   | 32 MB ペアで +173 MiB = **ft の 5.4 倍**（解凍 +183 MiB = 5.7 倍）→ 12 GB 換算 **約 65 GB 級**                                                                                                            | 50 GB 級               | < 1 GB                      | 実測は 4–5 倍見積りより悪い                                                                                 |
| K5  | C コア既知欠陥                       | **SEGFAULT 100 % 再現**（`total%256KiB∈{1,2,3}` 3/3、signal 11）。**Neo の生産デルタ経路（`delta_compress_files`）での到達も実証**（§4.3）                                                                | 再現（付録 C）         | 全ケースでエラーか正常動作  | Python 例外化されずプロセス死亡                                                                             |
| K6  | データ完全性検証                     | **なし**（コード監査: 圧縮/解凍のいずれにも事後検証経路が存在しない。`znn_neo_src_sha256` 等のキーは未定義）                                                                                              | なし                   | SHA‑256 端到端検証          | Phase 2 で新設                                                                                              |
| K7  | ダウンロード検証の追加 I/O           | `_sha256_of` フル再読込 **340 MB/s** → 10 GB あたり **+31 秒**の追加読み取り                                                                                                                              | +30–60 s/10 GB         | ゼロ（インライン）          | Plan 見込みと一致                                                                                           |
| K8  | ハッシュ 5 種 1 パス                 | `compute_hashes`（SHA256+AutoV1+CRC32+BLAKE3、256 MB）**278 MB/s** → 10 GB 換算 **約 38 秒**（単体 sha256 336 MB/s に対し 5 種合算のコスト +21 %）                                                        | 60–90 秒               | ≤ 15 秒                     | SHA 拡張なし CPU・1 スレッド                                                                                |
| K9  | スキャン 5,000 モデル（冷間）        | 5,016 エントリで **1.33 s**（中央値/3 回。dentry キャッシュ暖、`drop_caches` 不可のため「データキャッシュ冷間」）                                                                                         | 5–20 秒                | ≤ 2 秒                      | 本環境の高速ストレージでは既に目標内。ネットワークストレージ/低速機では Plan 見込み側へ寄る（環境依存が大） |
| K10 | スキャン（暖間・差分）               | 暖間全面再走査 **0.50 s**（`_SITE_CACHE` ホット。差分経路は存在しない）                                                                                                                                   | 全面再走査             | ≤ 100 ms                    | Phase 5 の永続インデックスで達成へ                                                                          |
| K11 | MoE ヘッダー解析（8 MB）             | `get_model_tensors` **中央値 930 ms**（read 5 ms + `json.loads` 506 ms + リスト構築 90 ms + その他。反復間 453–1,091 ms とブレ大 = 1 GiB RAM での GC/アロケータ負荷）                                     | 200–400 ms             | ≤ 40 ms                     | **見込みより大幅に悪い**。jiter 実測 10.6 ms（§2）により目標は達成可能                                      |
| K12 | モデル詳細ルートのイベントループ阻塞 | 修正前は K11 と同時間（**約 0.9 s**）サーバー全体が停止。Quick Win A1（本 Phase で実装・コミット済み）で executor 化 → 阻塞ゼロ                                                                           | 解析時間分ブロック     | ゼロ                        | A1 = 前半（完了）、Rust 化 = 後半（Phase 5）                                                                |
| K13 | 起動時 ZipNN 可用性                  | `ensure_zipnn()` 初回 **1.69 s**（内訳: `import zipnn`→torch 1.43 s、`zipnn_core` 単体 0.3 ms。ピーク RSS 223–241 MiB）。**Rust `mm_core` は 6 ms / 44 MiB**（Phase 0 hello world、abi3 import）          | プリビルド or C ビルド | import のみ < 50 ms         | mm_core は既に目標を 8 倍のマージンで達成                                                                   |
| K14 | 対応 dtype 数                        | **5 種**（f32/f16/bf16/fp8‑e4m3/fp8‑e5m2。`zipnn.py` compress() が他を ValueError で拒否することをコードで確認）                                                                                          | 5 種                   | 22 種以上                   | Phase 4                                                                                                     |
| K15 | 検索 keystroke → 描画                | **本環境では計測不能**（ブラウザ/GUI なし）。計測基盤（C5 performance mark + 5,000 モデル合成データ）は Phase 6 で常設し、そこでベースラインを記録する                                                    | 計測して記録           | ≤ 16 ms（P95）              | フロントエンド計測は計測基盤構築後                                                                          |
| K16 | プリビルドバイナリ                   | 現行: **linux‑x86_64 のみ・CPython 版別 ×6 本**（各 101,144 B、計 619,104 B。glibc ≥ 2.34）。Phase 0 成果物: **4 プラットフォーム 5 ターゲットすべて CI ビルド成功・import 疎通**（計 1.55 MiB、内訳 §5） | Linux のみ ×6          | 4 プラットフォーム・abi3 ×1 | サイズ予算 ≤4 MB/本を全成果物がクリア                                                                       |

---

## 4. 詳細結果

### 4.1 圧縮 / 解凍（K1/K2/K3）

**A. C コア直呼び**（`zipnn_core.zipnn_core` / `combine_dtype`、Gaussian σ=0.02 の合成テンソルバイト、2 スレッド。全ケースで往復 SHA‑256 一致）:

| dtype   | サイズ | 圧縮比 | 圧縮 MB/s | 解凍 MB/s | 圧縮ピーク | 解凍ピーク |
| ------- | ------ | ------ | --------- | --------- | ---------- | ---------- |
| bf16    | 32 MB  | 0.6623 | 800.7     | 899.5     | 92 MiB     | 103 MiB    |
| bf16    | 96 MB  | 0.6623 | 454.4     | 1,049.8   | 233 MiB    | 273 MiB    |
| bf16    | 192 MB | 0.6623 | 395.5     | 1,004.5   | 466 MiB    | 528 MiB    |
| f32     | 32 MB  | 0.8313 | 542.2     | 1,722.0   | 103 MiB    | 108 MiB    |
| f32     | 96 MB  | 0.8313 | 337.4     | 1,182.4   | 274 MiB    | 289 MiB    |
| f32     | 192 MB | 0.8313 | 361.8     | 1,049.8   | 531 MiB    | 561 MiB    |
| f16     | 32 MB  | 0.8434 | 612.8     | 1,381.7   | 103 MiB    | 108 MiB    |
| f16     | 96 MB  | 0.8434 | 462.0     | 1,359.7   | 275 MiB    | 290 MiB    |
| f16     | 192 MB | 0.8434 | 411.0     | 1,025.4   | 534 MiB    | 563 MiB    |
| fp8e4m3 | 32 MB  | 0.6697 | 410.4     | 1,057.4   | 92 MiB     | 103 MiB    |
| fp8e4m3 | 96 MB  | 0.6697 | 346.1     | 693.2     | 242 MiB    | 274 MiB    |
| fp8e4m3 | 192 MB | 0.6697 | 372.9     | 666.5     | 472 MiB    | 530 MiB    |

ピーク RSS の内訳は「Python 基底 ~36 MiB + 入力 + 平面バッファ + 出力」で、
**限界倍率（(peak − 基底) / 入力）は bf16 で 1.75× → 2.05× → 2.24×（32/96/192 MB）、
f32 で 2.57×（192 MB）**とサイズとともに漸増（チャンク表・アロケーションスラック）。
12 GB への線形外挿で **約 27–31 GB**（K1 の Plan 見込み「24 GB 超」を裏書きし、やや悪い）。

**B. Neo 端到端**（`py/compress.py` の実関数。torch 基底 RSS 241 MiB を差し引いた限界値で評価。解凍後は原本と SHA‑256 照合し **byte‑exact 一致**を確認）:

| モデル                          | 圧縮       | 解凍       | 圧縮比 | 圧縮ピーク（限界）     | 解凍ピーク（限界） |
| ------------------------------- | ---------- | ---------- | ------ | ---------------------- | ------------------ |
| 合成 bf16 64 MB（16 テンソル）  | 171.9 MB/s | 288.7 MB/s | 0.6623 | 409 MiB（+168 = 2.6×） | 352 MiB（+111）    |
| **実モデル** MiniLM f32 86.7 MB | 141.5 MB/s | 206.6 MB/s | 0.8192 | 456 MiB（+215 = 2.5×） | 404 MiB（+163）    |

C コア単体（395–800 MB/s）に対する e2e の律速（141–172 MB/s）は、テンソルごとの
Python ループ・`tensor.clone()`（C コアの入力破壊対策）・RAM dict 蓄積・`save_file`
書き戻しであり、Plan §4.3 のゼロコピー設計が回収する対象そのものである。
実モデル f32 の圧縮比 0.8192 は README の「realistic checkpoints 60–80 %」の f32 側と整合。

### 4.2 デルタ圧縮（K4）

32 MB f32 ペア（全要素に小さなドリフト = フルファインチューン相当。`delta_compress_files` →
`delta_decompress_file` → 復元 SHA‑256 **byte‑exact 一致**）:

| フェーズ | 時間    | ピーク RSS          | 限界倍率（ft 比）   |
| -------- | ------- | ------------------- | ------------------- |
| 圧縮     | 0.349 s | 414 MiB（基底 241） | **+173 MiB = 5.4×** |
| 解凍     | 0.214 s | 424 MiB（基底 241） | **+183 MiB = 5.7×** |

デルタサイズ比 0.6891（このフィクスチャはバイトの ~6 % が差分。実 FT では一般にさらに小さい）。
両ファイル全文の `f.read()` + パディングコピー + numpy XOR コピー + C コア平面、という
Plan §1.2.2 #5 の内訳通りで、**実測倍率は「4–5 倍」の見込みをやや上回る**。
12 GB ペア換算で 65 GB 級 = Plan の「50 GB 級」見積りと同オーダー。

### 4.3 既知欠陥の再現（K5 — Plan 付録 C）

| ケース                                                                                                                   | 結果                                           |
| ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| 生 C コア: `zipnn_core(header, bytearray(262144+k), 4, 1, 220, …)`、k ∈ {1,2,3}                                          | **3/3 SIGSEGV（exit −11）100 % 再現**          |
| **生産経路**: `py/compress.py delta_compress_files()` に、パディング後総長 % 256 KiB = 1 の手組み safetensors ペアを投入 | **SIGSEGV（Python 例外化されずプロセス死亡）** |

2 番目は付録 C.5 の「デルタ圧縮は危険（到達可能）」を **Neo の実ルート経由で実証**した
もの（ヘッダー長を解析的に調整した手組みペアで総長を制御。safetensors 公式
シリアライザはヘッダーを 8 バイト境界に整列するため、奇数剰余は任意ヘッダー長を
持てる実ファイル — 別ツール出力や手編集 — で生じる）。Rust 版では同ケースを
回帰テストに固定する（Phase 3 の完了条件）。

**付録 C.3 の全ケース行列**（22 ケース: dtype32 クラッシュ 8 ＝ 1st/2nd チャンク境界
×{1,2,3} ＋低エントロピー、対照 9 ＝チャンク境界ちょうど・%4≥4 最終チャンク・64/67 B、
dtype16 経路 5 ＝境界・奇数長）は `scripts/bench/bench_c_defects.py` で一括再実行でき、
証跡を [`scripts/bench/results/c_defects.json`](../scripts/bench/results/c_defects.json)
にコミットしている（2026‑09‑23 実行: **SEGFAULT 8/8・対照往復一致 14/14・逸脱 0** ＝
`appendixCReproduced: true`。別サンドボックスでの独立実行でも同一結果）。

### 4.4 スキャン（K9/K10）

5,000 モデル合成ライブラリ（7,532 ファイル、md サイドカー 1,028・プレビュー 1,503）で
`ModelManager.scan_models()` 全面走査（6 タイプ合計 5,016 エントリ）:

| 計測                     | 時間          | 備考                                                       |
| ------------------------ | ------------- | ---------------------------------------------------------- |
| 冷間（初回、3 回中央値） | **1.332 s**   | `drop_caches` 不可（コンテナ）のため dentry キャッシュは暖 |
| 暖間（同プロセス再走査） | **0.501 s**   | `_SITE_CACHE` ホット。**差分経路はなく毎回全面走査**       |
| `scan_hygiene`           | 0.235–0.250 s | 孤立サイドカー 1・空フォルダ 1 を正しく検出                |

本環境（高速ローカルストレージ・2 vCPU）では K9 目標（≤2 s）内に既に入っているが、
Plan の見込み（5–20 s）はネットワークストレージや HDD を含む実環境の話であり、
`_model_site_info_of` の 4 KB 先頭読みがネットワーク越しに 1,028 回走る構成では
容易にその域に入る（コード上の根拠: manager.py の `_SITE_CACHE` 注释）。
K10（暖間 ≤100 ms）は現行構造では原理的に未達（全面再走査 0.5 s）で、
Phase 5 の永続インデックス + 差分スキャンが必要条件。

### 4.5 ヘッダー解析（K11/K12）

8.42 MB MoE ヘッダー（64,491 テンソル、DeepSeek 風命名）で `py/utils.py` の実関数:

| ステップ                       | 中央値     | 備考                                                |
| ------------------------------ | ---------- | --------------------------------------------------- |
| ヘッダー読み取り（8 B + 本体） | 5.4 ms     | `comfy.utils.safetensors_header` 相当               |
| `json.loads`                   | 506 ms     | 64k dict の materialize = コストの主体              |
| テンソルリスト構築             | 89.5 ms    | `get_model_tensors` のループ                        |
| **`get_model_tensors` 合計**   | **930 ms** | 反復間 453–1,091 ms（1 GiB 環境の GC 負荷でブレ大） |

- Plan 見込み（200–400 ms）を **2 倍以上**上回る。K11 目標 ≤40 ms に対し、
  現行 Python 経路は 23 倍の距離。§2 の jiter 実測（10.6 ms）は Rust 化で
  目標が十分達成できることを示す。
- **副次発見**: `get_model_metadata()` の 1 MiB ガードにより、ヘッダーが 1 MiB を
  超える MoE モデルでは `__metadata__`（ZipNN 圧縮記録を含む）が**黙って空扱い**になる。
  8 MB ヘッダーで実測確認（`metadataEmptyDueToGuard: true`）。Phase 5 の B4
  （ヘッダー解析内製化）で 32 MiB ガードへ統一すべき候補。
- K12: Quick Win A1 適用前、この 0.93 s はモデル詳細を開くたびに
  **イベントループ全体（WebSocket 配信含む）を停止**させていた。A1 により
  ルートは executor 経由になり阻塞は解消（Quick Win A1、commit 047568b）。

### 4.6 ハッシュ（K7/K8）

256 MB のランダムファイル（1 回生成、ページキャッシュ暖）:

| 経路                                        | 時間    | スループット | 備考                                                  |
| ------------------------------------------- | ------- | ------------ | ----------------------------------------------------- |
| `identify.compute_hashes`（5 算法 1 パス）  | 0.920 s | **278 MB/s** | SHA256+AutoV2+AutoV1+CRC32+BLAKE3。値の相互一致を確認 |
| `download._sha256_of`（検証フル再読込）     | 0.753 s | 340 MB/s     | K7 の「追加 I/O」実体                                 |
| 素の hashlib sha256（参照、1 MiB チャンク） | 0.762 s | 336 MB/s     | 5 算法合算の追加コストは約 +21 %                      |

10 GB 換算: 5 種 1 パス ≈ **38 s**（K8 目標 ≤15 s は Rust 並列 + SHA‑NI 前提。
本 CPU は SHA 拡張なしで AVX2 経路）、ダウンロード後検証の再読込 ≈ **+31 s**
（K7 目標「ゼロ」= インライン化で丸ごと消去）。SHA256 値は 3 経路で完全一致
（`compute_hashes` の正しさの相互検証にもなっている）。

### 4.7 起動・可用性（K13）

| 経路                                                | 時間        | ピーク RSS |
| --------------------------------------------------- | ----------- | ---------- |
| `ensure_zipnn()` 初回（プレビルド経路、実測の全体） | **1.687 s** | 241 MiB    |
| ┗ `import zipnn_core`（C 拡張単体）                 | 0.3 ms      | 17 MiB     |
| ┗ `import zipnn`（Python 層 → **torch を道連れ**）  | 1.432 s     | 223 MiB    |
| **`py/native.py` → `import mm_core`（Rust abi3）**  | **6 ms**    | 44 MiB     |

現行の遅さの実体は C コアでも vendored パッケージでもなく **torch の import**
（ComfyUI プロセスでは torch は既にロード済みのため実運用では隠れるが、
「ZipNN 可用性」の初回コストとしてのベースラインは上記の通り）。
Rust コアは hello world 段階で既に目標（<50 ms）を達成しており、
Phase 2 以降も API 増でこの桁が変わる要因はない（依存は pyo3 のみ）。

---

## 5. Phase 0 ネイティブ成果物の実測（サイズ予算）

release profile（`lto="fat"` / `codegen-units=1` / `strip` / `panic="unwind"`）の
hello‑world abi3 バイナリ（予算: ≤4 MB/本、計 ≤20 MB）。
**4 プラットフォーム（5 ターゲット）すべて CI（native.yml @ dev、commit 81854f5、
2026‑09‑23）でビルド成功・サイズゲート通過・import 疎通済み**:

| ターゲット                   | ビルド経路                                          | サイズ（CI 実測） | 予算判定     | import 疎通（CI）                                                            |
| ---------------------------- | --------------------------------------------------- | ----------------- | ------------ | ---------------------------------------------------------------------------- |
| linux‑x86_64（glibc ≥2.28）  | cargo‑zigbuild                                      | 410,416 B         | ✅ 10 分の 1 | CPython **3.10 / 3.11 / 3.13**（abi3‑import ジョブ = abi3 主張の機械的検証） |
| linux‑aarch64（glibc ≥2.28） | cargo‑zigbuild                                      | 383,824 B         | ✅           | ELF + glibc 上限 2.28 を readelf で検証（実行は対象 arch のためビルド検証）  |
| windows‑x86_64（MSVC）       | maturin（windows‑latest）                           | **163,840 B**     | ✅           | CPython 3.11（`abi3 import OK: 1 0.3.0-alpha.0+81854f536`）                  |
| macos‑universal2             | maturin universal2 = 両 arch + lipo（macos‑latest） | **666,032 B**     | ✅           | CPython 3.11 arm64（`lipo -info`: x86_64 + arm64 を確認）                    |
| **合計**                     | —                                                   | **1,624,112 B**   | ✅ ≤20 MB    | —                                                                            |

- 参考: ローカル native ビルド（mold、glibc 2.36）385,808 B、ローカル zigbuild 410,336 B
  （CI との ±80 B 差はビルド環境由来。全成果物が予算の 1/6 以下）。
- maturin wheel は `mm_core-0.3.0a0-cp310-abi3-*`（abi3 タグ = CPython ≥3.10 単一バイナリ）。
- `core_version()` がビルド元コミット（`+81854f536`）を CI ランナーでも正しく返しており、
  build.rs の git ハッシュ埋め込みが全プラットフォームで機能することを確認済み。

---

## 6. 観察と結論（Phase 1 以降への申し送り）

1. **付録 C の欠陥はこの環境でも 100 % 再現**し、さらに **Neo の生産デルタ経路で
   到達可能**であることを実証した（§4.3）。K5 の重大度評価は実測で裏付けられた。
2. **ヘッダー解析は Plan 見込みより深刻**（930 ms vs 200–400 ms）。K11/K12 の
   優先度は計画通りで正しかった。加えて `get_model_metadata` の 1 MiB ガードが
   大型 MoE でメタデータを黙って落とす挙動を発見（§4.5、Phase 5 B4 の設計入力）。
3. **json.loads が支配的コスト**（506/930 ms）。jiter はその 48 倍速で、
   選定は計測により確定した（§2）。
4. **e2e の律速は Python/torch 層**（C コア比 2.3–2.8 倍遅い）。Rust 化の速度目標
   （K2 ≥1.5 倍 / K3 ≥2 倍）は **e2e 値を基点**に判定する。
5. **ピーク RAM 倍率の実測は Plan 見積りよりやや悪い**（圧縮 2.2–2.6×、デルタ 5.4–5.7×）。
   K1/K4 の「<1 GB」目標の価値はむしろ大きい。
6. **K9 は環境依存が極めて大きい**（本環境の冷間 1.33 s vs Plan 見込み 5–20 s）。
   Phase 5 の完了判定は「参照機 + ネットワークストレージ」構成での再計測を必須とする。
7. `ensure_zipnn` の初回 1.69 s はほぼ torch import。Rust コア（6 ms）への移行は
   K13 を桁で改善する（§4.7）。
8. 本環境（1 GiB RAM）では 12 GB 級の直接計測は不可能なため、K1/K4 の 12 GB 換算は
   **32–192 MB 実測の線形外挿**である（倍率の線形性は 3 点で確認済み）。
   参照機での裏取りは Phase 2 の L4 テストと併せて行う。

---

## 7. 再現手順

```bash
pip install numpy safetensors torch blake3
# 実モデル（任意）: ModelScope の unsloth/all-MiniLM-L6-v2 model.safetensors を
# ローカルに取得し、REAL_MODEL に渡す。
FIXTURES=/tmp/mm-bench REAL_MODEL=/path/model.safetensors ./scripts/bench/run_all.sh
# → scripts/bench/results/*.json（env ブロックに実行環境が自動記録される）
```

`scripts/bench/results/` の JSON は本実行（2026‑09‑23、上記環境）の証跡としてコミットする。
