# Development / verification dependencies (harness only).
#
# The runtime dependency list is ../requirements.txt - ComfyUI itself provides
# aiohttp and Pillow at runtime, so they are deliberately NOT in there. The
# verification harness (harness/py_probe.py, harness/serve.py) runs outside
# ComfyUI and therefore needs them explicitly.
aiohttp>=3.9
pillow>=10
pyyaml>=6
requests>=2.31
markdownify>=0.14.0
huggingface_hub>=0.34.0,<1.32.0
